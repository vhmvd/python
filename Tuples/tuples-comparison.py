#Starts from left side and compares indvidually. If false, returns flase, if true, returs true else
#move to the the next element in the tupple for comparison

a = (1, 45, 23)
b = (0, 443, 43)
print(a<b)